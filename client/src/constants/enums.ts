export enum ToastTypes {
  ERROR = 'error',
  SUCCESS = 'success',
  INFO = 'info',
  DEFAULT = 'default',
  WARNING = 'warning',
}

export enum UserRoles {
  ADMIN = 'admin',
  PROJECT_OWNER = 'projectOwner',
  TEAM_LEAD = 'teamLead',
  MEMBER = 'member',
}

export enum CrudOperations {
  CREATE = 'add',
  UPDATE = 'edit',
  READ = 'view',
  DELETE = 'delete',
}
